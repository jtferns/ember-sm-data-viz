import d3Cloud from 'dummy/utils/d3-cloud';
import { module, test } from 'qunit';

module('Unit | Utility | d3 cloud');

// Replace this with your real tests.
test('it works, for now', function(assert) {
  let result = d3Cloud();
  assert.ok(result);
});
